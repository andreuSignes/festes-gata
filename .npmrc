# Defaults to the public npm registry; change here if the project
# ever pulls from a private mirror.
registry=https://registry.npmjs.org/

# Fail the install when the active Node doesn't satisfy
# `engines.node` in package.json. Belt-and-braces on top of the
# version pin in CI (Node 22) and the local `.nvmrc`.
engine-strict=true
